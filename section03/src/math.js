// math 모듈

export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

export default function multilply(a, b) {
  return a * b;
}

// 변수와 함수의 이름이 같다면 변수와 동일한 이름의 함수가 실행
// module.exports = {
//     add,
//     sub,
// };

//export { add, sub };
